import Retail from "../../../components/services/Retail";

export const metadata = {
  title: "Retail Orders — TheTiffins",
  description:
    "Order individual tiffins on demand. Fresh home-cooked meals ready for pick-up or delivery.",
};

export default function RetailPage() {
  return <Retail />;
}
