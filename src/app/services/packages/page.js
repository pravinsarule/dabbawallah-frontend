import PackagesAndPlan from "../../../components/services/PackagesAndPlan";

export const metadata = {
  title: "Packages & Plans — TheTiffins",
  description:
    "Choose from flexible tiffin packages — daily, weekly, and monthly plans for students and professionals.",
};

export default function PackagesPage() {
  return <PackagesAndPlan />;
}
