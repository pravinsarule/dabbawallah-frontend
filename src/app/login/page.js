import Login from "../../components/auth/Login";

export const metadata = {
  title: "Sign In — TheTiffins",
  description: "Sign in to manage your tiffin subscription and orders.",
};

export default function LoginPage() {
  return <Login />;
}
