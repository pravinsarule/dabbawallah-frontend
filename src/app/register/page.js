import Register from "../../components/auth/Register";

export const metadata = {
  title: "Create Account — TheTiffins",
  description: "Join TheTiffins community as a customer or register your kitchen as a tiffin provider.",
};

export default function RegisterPage() {
  return <Register />;
}
